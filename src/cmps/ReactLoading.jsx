import React from 'react'
import ReactLoading from 'react-loading'
 
const Example = ({ type, color }) => (
    <ReactLoading type={type} color={color} height={400} width={400} />
)
 
export default Example